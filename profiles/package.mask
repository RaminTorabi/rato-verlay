# Atoms in this file are deliberately masked: the ebuild is shipped
# in the overlay (so you can pin to it if you know you want it) but
# does not get installed by default. Unmask any of them in your own
# /etc/portage/package.unmask/rato-verlay if you want to opt in.

# Ramin Torabi <16882282+RaminTorabi@users.noreply.github.com> (2026-04-25)
# CloudWatch Agent 1.300067.0 was tagged upstream but is not the
# release marked as latest stable (latest stable: 1.300066.1). Mask
# the newer tag so a default `emerge` keeps picking 1.300066.1; unmask
# explicitly if you want to track the upstream tag aggressively.
=app-admin/amazon-cloudwatch-agent-1.300067.0

# Ramin Torabi <16882282+RaminTorabi@users.noreply.github.com> (2026-05-05)
# amazon-efs-utils-bin is a prebuilt-binary repackaging of AWS's
# official AL2023 RPM. The source-build sibling (net-fs/amazon-efs-utils,
# 9999 live ebuild) is the default install path; the -bin variant is
# kept available for hosts that cannot afford a Rust + Python build.
# Unmask explicitly if -bin is what you want.
net-fs/amazon-efs-utils-bin

# Ramin Torabi <16882282+RaminTorabi@users.noreply.github.com> (2026-05-24)
# VictoriaMetrics cluster mode (vminsert + vmselect + vmstorage) is shipped
# in the overlay for completeness and to give a clean upgrade path if a
# fleet ever outgrows the single-node sibling. It is NOT the recommended
# default: for ingestion rates below ~1M samples/sec the single-node
# variant (app-metrics/victoria-metrics{,-bin}) is simpler to operate and
# uses ~3x less memory. Unmask explicitly via /etc/portage/package.unmask
# if cluster mode is what you actually want; otherwise the single-node
# pair stays the daily driver.
#
# Same masking rationale applies to both the source-build and prebuilt
# variants — they are siblings, not alternatives, and the !! blocker
# between them prevents Portage from picking both at once.
app-metrics/victoria-metrics-cluster
app-metrics/victoria-metrics-cluster-bin

# Oldest of the newest 5 ebuilds kept by the daily retention
# policy. Hard-masked so a default emerge resolves to a newer
# version; unmask explicitly to pin this floor.

# retention-floor: app-admin/amazon-ssm-agent
# Oldest of the newest 5 ebuilds kept by the daily retention
# policy. Hard-masked so a default emerge resolves to a newer
# version; unmask explicitly to pin this floor.
=app-admin/amazon-ssm-agent-3.3.4177.0

# Oldest of the newest 5 ebuilds kept by the daily retention
# policy. Hard-masked so a default emerge resolves to a newer
# version; unmask explicitly to pin this floor.

# Oldest of the newest 5 ebuilds kept by the daily retention
# policy. Hard-masked so a default emerge resolves to a newer
# version; unmask explicitly to pin this floor.

# Oldest of the newest 5 ebuilds kept by the daily retention
# policy. Hard-masked so a default emerge resolves to a newer
# version; unmask explicitly to pin this floor.

# Oldest of the newest 5 ebuilds kept by the daily retention
# policy. Hard-masked so a default emerge resolves to a newer
# version; unmask explicitly to pin this floor.

# Oldest of the newest 5 ebuilds kept by the daily retention
# policy. Hard-masked so a default emerge resolves to a newer
# version; unmask explicitly to pin this floor.

# Oldest of the newest 5 ebuilds kept by the daily retention
# policy. Hard-masked so a default emerge resolves to a newer
# version; unmask explicitly to pin this floor.

# retention-floor: dev-util/kiro-bin
# Oldest of the newest 5 ebuilds kept by the daily retention
# policy. Hard-masked so a default emerge resolves to a newer
# version; unmask explicitly to pin this floor.
=dev-util/kiro-bin-0.12.292

# retention-floor: dev-util/kiro-cli-bin
# Oldest of the newest 5 ebuilds kept by the daily retention
# policy. Hard-masked so a default emerge resolves to a newer
# version; unmask explicitly to pin this floor.
=dev-util/kiro-cli-bin-2.5.0

# weekly-verify 2026-06-13: all SRC_URIs unreachable; hard-masking (see s3://ami-gentoo-artifacts-320513496842-eu-north-1/build-logs/i-023bb24cfd1a13aa4/weekly-verify/dev-util_kiro-cli-bin_2.6.0-r1.url.log)
=dev-util/kiro-cli-bin-2.6.0-r1
